var Browser = function(){
  var that;
  that.Tabs = function(){
    var that;
    function makeBrowser() { 
      var browser = document.createElement("iframe");
      $(browser).addClass("browser");
      return browser;
    }
    that.create = function(uri){
      
    }
  };

  return that;
};
